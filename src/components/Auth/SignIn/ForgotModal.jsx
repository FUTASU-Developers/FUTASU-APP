import React from 'react'
import { StyleSheet, Text, View, Modal } from 'react-native'

const ForgotModal = ({visibility}) => {
    return (
        <View>
            <Modal
                visible={visibility}
            >

            </Modal>
        </View>
    )
}

export default ForgotModal

const styles = StyleSheet.create({})
